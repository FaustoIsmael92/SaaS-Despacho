# Copia este archivo a .env y rellena los valores. NUNCA commitear .env al repositorio.
# Si credenciales reales llegaron a Git, rótalas en Supabase y en Vercel.
#
# Base de datos (Supabase PostgreSQL)
# Opción A – Conexión directa (migraciones; requiere IPv6 o add-on IPv4):
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres"
# Opción B – Session mode (si la directa falla por auth o red). En Dashboard: Connect → Session mode.
# El usuario es postgres.[PROJECT_REF], no solo postgres:
# DATABASE_URL="postgres://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres"

# Supabase (cliente público para Auth y RLS)
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT_REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[ANON_KEY]"

# Opcional: para operaciones server-side con service role (solo en API routes seguras)
# SUPABASE_SERVICE_ROLE_KEY="[SERVICE_ROLE_KEY]"

# Cron jobs (reprogramar tareas vencidas y generar actividades mensuales).
# En Vercel: definir CRON_SECRET y los crons enviarán Authorization: Bearer <CRON_SECRET>
# CRON_SECRET="[generar con: openssl rand -hex 32]"
