export const APP_NAME = "Despacho Contable";

export const ROUTES = {
  HOME: "/",
  LOGIN: "/login",
  REGISTRO: "/registro",
  DASHBOARD: "/dashboard",
} as const;

export const PUBLIC_PATHS = ["/", "/login", "/registro"] as const;
